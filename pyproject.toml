[project]
name = "tg-run"
version = "0.1.0"
description = "Telegram bot to open a terminal with Claude Code in a chosen project folder on Windows"
requires-python = ">=3.11"
dependencies = [
    "aiogram>=3.29.1",
    "python-dotenv>=1.2.2",
]

[tool.uv]
package = false
